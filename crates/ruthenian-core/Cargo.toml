[package]
name = "ruthenian-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Productive Russian morphology as pure rules, in Ruthenian orthography"

# ruthenian-orthography only. No third-party dependencies; the `no_dependencies`
# guard enforces it.
[dependencies]
ruthenian-orthography = { path = "../ruthenian-orthography" }
