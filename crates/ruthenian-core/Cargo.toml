[package]
name = "ruthenian-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "The productive morphology of Ruthenian as pure rules"

# ruthenian-orthography only. No third-party dependencies; the `no_dependencies`
# guard enforces it.
[dependencies]
ruthenian-orthography = { path = "../ruthenian-orthography" }
